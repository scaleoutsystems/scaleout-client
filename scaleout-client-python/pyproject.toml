[project]
name = "scaleout"
version = "1.0.3"
description = "A client library for the Scaleout Edge"
authors = [{ name = "Scaleout Systems AB" }]
requires-python = ">=3.11, <3.14"
readme = "README.rst"
license = "Apache-2.0"
dependencies = [
    "PyYAML~=6.0",
    "numpy>=1.21.6",
    "psutil",
    "scaleoututil==1.0.3",
    "click==8.3.1",
    "urllib3>=2.6.0",
]

[project.urls]
Homepage = "https://scaleoutsystems.com"
Documentation = "https://docs.scaleoutsystems.com/en/latest/"

[project.optional-dependencies]
test = ["pytest~=9.0", "responses>=0.25.0"]

[dependency-groups]
dev = [
    "pytest~=9.0",
    "mypy~=1.0",
    "responses>=0.25.0",
]

[project.scripts]
scaleout = "scaleout.cli.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
scaleoututil= { path = "../scaleout-util", editable = true }
